# Copy outside the repository, fill values, and chmod 0600.
GPU_WORKER_API_TOKEN=<same-secret-configured-in-backend-client>
WORKER_REGISTRATION_TOKEN=<worker-registration-secret>
FILMFORGE_BACKEND_URL=https://backend.example.invalid
WORKER_API_AUTH_MODE=required
WORKER_PUBLIC_URL=https://gpu0.example.invalid
