# Copy outside the repository, fill values, and chmod 0600.
# This endpoint must make the backend's real bearer-sending worker request and
# return filmforge.worker-cutover-probe.v1 proof after the worker restart.
FILMFORGE_BACKEND_CUTOVER_PROBE_URL=https://backend.example.invalid/api/internal/worker-cutover-probe
FILMFORGE_BACKEND_CUTOVER_PROBE_TOKEN=<independent-cutover-probe-secret>
